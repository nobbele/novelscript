[ do test / don't do test ]
if choice = 1
    _: What is the number?
    
    if number = 1
        if another_number > 0
            Foo: Another number is more than 0
        else if another_number < 0
            Foo: Another number is less than 0
        else
            Foo: It is 0
        end
    end
    if number != 1
        Foo: It is not 1
    end

    _: Cool
end